Stores user inputs including category, item-name and price in localStorage for each item
Adds every new item to storage.
Reads from storage, and displays all stored items on page.
Buttons to submit data from all fields, using the "required" property, reload (refresh) page, and clear localStorage.
Buttons to sort stored items by price, ascending and descending.
